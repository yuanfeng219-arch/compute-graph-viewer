version: "__APP_VERSION__"
name: rw-offline-macos
type: seatbelt
os: macos

# Read/Write access with NO network.
# Use case: Local-only tasks, processing sensitive data.

filesystem:
  read:
    # Working directory
    - "."
    # System binaries and libraries
    - "/usr"
    - "/bin"
    # Homebrew (Apple Silicon + Intel)
    - "/opt/homebrew"
    - "/usr/local"
    # System frameworks
    - "/System/Library"
    - "/Library/Frameworks"
    # macOS temp and runtime
    - "/private/tmp"
    - "/private/var/folders"
    # Devices
    - "/dev/null"
    - "/dev/zero"
    - "/dev/random"
    - "/dev/urandom"
    - "/dev/tty"
    # User tools (uvx, pipx)
    - "~/.local"
    - "~/.cache/uv"
    - "~/.npm"  # npx cache
  write:
    - "."
    - "/private/tmp"
    - "/private/var/folders"
    - "~/.cache/uv"  # uvx package cache
    - "~/.npm"  # npx package cache
    - "~/.local"  # uvx tools install here
    - "/dev/null"
    - "/dev/tty"
  hidden:
    # Environment and secrets
    - ".env"
    - "**/.env"
    - "**/.env.*"
    - "**/secrets.yml"
    - "**/secrets.yaml"
    - "**/credentials.json"
    # Git security
    - "**/.git/hooks"
    - "**/.git/config"
    # SSH and GPG
    - "~/.ssh"
    - "~/.gnupg"
    - "~/.git-credentials"
    # Shell configs (prevent persistence)
    - "~/.bashrc"
    - "~/.zshrc"
    - "~/.profile"
    - "~/.bash_profile"
    - "~/.zprofile"
    # Cloud credentials
    - "~/.aws"
    - "~/.config/gcloud"
    - "~/.azure"
    - "~/.kube"
    # Container and package manager credentials
    - "~/.docker/config.json"
    - "~/.netrc"
    - "~/.npmrc"
    - "~/.pypirc"
    - "~/.gem/credentials"
    - "~/.cargo/credentials"
    # Private keys
    - "*.pem"
    - "*.key"
    - "*_rsa"
    - "*_ed25519"
    - "*_ecdsa"
    - "*_dsa"
    - "*.p12"
    - "*.pfx"
    # macOS keychain
    - "~/Library/Keychains"

network:
  remote: []
  local: []
